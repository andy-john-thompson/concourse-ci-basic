#!/bin/sh
echo "stub to represent a stage that tests the artifact - eg security scan/whatever.."
echo "stub shows the contents of the built artifact, passed in from compilation:"
ls my-build-artifacts/*
cat "my-build-artifacts/product.txt"